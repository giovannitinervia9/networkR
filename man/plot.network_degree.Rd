% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_degree.R
\name{plot.network_degree}
\alias{plot.network_degree}
\title{Plot Degree Distribution for \code{network_degree} Object}
\usage{
\method{plot}{network_degree}(x, which = 1:3, ...)
}
\arguments{
\item{x}{An object of class \code{network_degree}.}

\item{which}{An integer vector specifying which plots to display. Choices
are:
\itemize{
\item \code{1}: Density plot of node degree distribution.
\item \code{2}: ECDF of the node degrees.
\item \code{3}: Log-log plot of the survival function.
Defaults to showing all three plots (\code{1:3}).
}}

\item{...}{Additional arguments (not used here).}
}
\value{
Invisibly returns a list of \code{ggplot} objects for the selected plots.
}
\description{
This is a plot method for objects of class \code{network_degree}, providing
visualization of the degree distribution as density, empirical cumulative
distribution function (ECDF), and log survival function (log-log plot).
}
\details{
The function generates three plots:
\itemize{
\item \strong{Density Plot} (Plot 1): Shows the relative frequency of each degree.
\item \strong{ECDF Plot} (Plot 2): Shows the cumulative distribution of degrees.
\item \strong{Log Survival Plot} (Plot 3): A log-log plot of the survival function
with a linear fit, useful for assessing the tail behavior of the degree distribution.
}
}
\examples{
# Create a network and calculate degree information
graph <- list(c(1, 2), c(2, 3), c(3, 1), c(1, 4))
network <- graph_to_network(graph)
degree_info <- network_degree(network)

# Plot all available degree distribution graphs
plot(degree_info)

# Plot only the density and ECDF plots
plot(degree_info, which = 1:2)

}
